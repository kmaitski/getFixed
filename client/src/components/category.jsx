import React from 'react';

const Category = (props) => {
  // incoming props = {key, category, index}
  return (
    // need Redirect for onClick?
    <div onClick={}>
      // {props.category}
    </div>
  )
}

export default Category;